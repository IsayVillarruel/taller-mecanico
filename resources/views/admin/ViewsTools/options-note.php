@if($consult != null)
    <a href="{{ $consult }}" class="btn btn-sm btn-primary"><i class="fas fa-eye"></i></a>
@endif
@if($send != null)
    <a href="{{ $send }}" class="btn btn-sm btn-primary"><i class="fas fa-paper-plane"></i></a>
@endif

